//该文件用于创建reducer函数

export function collReducer(preState,action){

   
    const {type,data} = action

    switch (type) {

        case 'Iscollapsed':
            
          return !da
    
        default:
            break;
    }
} 